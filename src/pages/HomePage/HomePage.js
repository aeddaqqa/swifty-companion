import { Text } from "react-native";
import styled from "styled-components";

const StyledHome = styled.View`
    height: 100%;
`;

const Home = () => {
    return (
        <StyledHome>
            <Text></Text>
        </StyledHome>
    );
};

export default Home;
